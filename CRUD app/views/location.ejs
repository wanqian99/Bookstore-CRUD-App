<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>test</title>
    <link rel="stylesheet" href="/css/styles.css">
    <script type = "text/javascript" src="/js/scripts.js"></script>
</head>
    <body>
        <header>
            <div class="topnav">
                <a href="/">Home</a>
                <a href="book">Books</a>
                <a href="author">Authors</a>
                <a href="publisher">Publishers</a>
                <a href="location" class="active">Books published Location</a>
                <a href="category">Books Category</a>
                <a href="format">Books Format</a>
                <a href="cover">Books Cover</a>
                <a href="inventory">Books Inventory</a>
            </div>
        </header>

        <h1>Books' Published Location page</h1>
        <p>This page consists of a table that contains the data in the location table.</p>
        
        <form action="/location" method="POST">
            <input type="text" name="keyword" placeholder="Search by published location">
            <input type="submit" value="Search">
        </form>

        <button onclick="sortTable(1)">Sort by city</button>
        <button onclick="sortTable(2)">Sort by country</button>
            
        <form action="/location" method="GET">
            <br>
            <table border="1" id="table">
                <tr>
                    <th>id</th>
                    <th>City</th>
                    <th>Country</th>
                </tr>
                <% if (location.length == 0) { %>
                    <p>No location named <%=search%></p>
                <% } else { %>
                    <% for(var i = 0; i < location.length; i++) { %>
                    <tr>
                        <td><%=location[i].id%></td>
                        <td><%=location[i].city%></td>
                        <td><%=location[i].country%></td>
                    </tr>
                    <% } %>
                <% } %>
            </table>
        </form>
    </body>
</html>